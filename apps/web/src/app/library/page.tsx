import type { Metadata } from "next";

import { LibraryDeckCard } from "@/components/library-deck-card";
import { PublicSiteFooter } from "@/components/public-site-footer";
import { PublicSiteFooterCta } from "@/components/public-site-footer-cta";
import { PublicSiteNav } from "@/components/public-site-nav";

export const metadata: Metadata = {
  title: "Output Library",
  description:
    "Download real PPTX decks generated by Basquio. Healthcare analysis, executive summaries, and marketplace deep dives. See the quality before you sign up.",
  alternates: { canonical: "https://basquio.com/library" },
  openGraph: {
    title: "Output Library | Basquio",
    description: "Download real PPTX decks generated by Basquio. See the quality before you sign up.",
    type: "website",
    images: [{ url: "/library/analysis/analysis.006.png", width: 960, height: 540 }],
  },
};

const decks = [
  {
    title: "Healthcare AI Platform — Performance & Growth Analysis",
    tier: "Deck",
    slideCount: 15,
    description:
      "Board-level analysis covering ARR trajectory, departmental performance, clinical outcomes, and growth strategy across 6 clinical departments.",
    heroImage: "/library/analysis/analysis.006.png",
    slides: Array.from({ length: 15 }, (_, i) => `/library/analysis/analysis.${String(i + 1).padStart(3, "0")}.png`),
    downloadHref: "/library/analysis/deck.pptx",
  },
  {
    title: "Payment Infrastructure — Executive Summary",
    tier: "Memo",
    slideCount: 4,
    description:
      "Quick executive readout: $19.7M processing revenue, regional breakdown, and three strategic recommendations. Generated in under 2 minutes.",
    heroImage: "/library/exec-summary/exec-summary.002.png",
    slides: Array.from(
      { length: 4 },
      (_, i) => `/library/exec-summary/exec-summary.${String(i + 1).padStart(3, "0")}.png`,
    ),
    downloadHref: "/library/exec-summary/deck.pptx",
  },
  {
    title: "E-Commerce Marketplace — Deep Dive Analysis",
    tier: "Deep-Dive",
    slideCount: 10,
    description:
      "Full marketplace diagnostic: category economics, return rate analysis, seller comparison, and regional performance matrix.",
    heroImage: "/library/deep-analysis/deep-analysis.003.png",
    slides: Array.from(
      { length: 10 },
      (_, i) => `/library/deep-analysis/deep-analysis.${String(i + 1).padStart(3, "0")}.png`,
    ),
    downloadHref: "/library/deep-analysis/deck.pptx",
  },
] as const;

export default function LibraryPage() {
  return (
    <div className="page-shell public-page">
      <PublicSiteNav />

      <section className="page-hero">
        <div className="stack">
          <p className="section-label">Output library</p>
          <h1>Real decks. Real analysis. Made by Basquio.</h1>
          <p className="page-copy">
            Every deck below was generated by Basquio from uploaded data. Browse the slides, then download the PPTX and
            open it yourself.
          </p>
        </div>
      </section>

      <div className="library-grid">
        {decks.map((deck) => (
          <LibraryDeckCard
            key={deck.title}
            title={deck.title}
            tier={deck.tier}
            slideCount={deck.slideCount}
            description={deck.description}
            heroImage={deck.heroImage}
            slides={[...deck.slides]}
            downloadHref={deck.downloadHref}
          />
        ))}
      </div>

      <section className="library-context">
        <p className="library-context-lead">
          Every chart is computed from uploaded data. Every claim references the source file. Nothing is hallucinated.
        </p>
        <div className="library-proof-points">
          <span>Charts computed from your actual numbers</span>
          <span className="library-proof-dot" aria-hidden="true" />
          <span>Editable in PowerPoint, Keynote, or Slides</span>
          <span className="library-proof-dot" aria-hidden="true" />
          <span>Source note on every chart</span>
        </div>
      </section>

      <PublicSiteFooterCta
        eyebrow="See for yourself"
        title="Upload your data. Get a deck like these."
        copy="30 free credits. No credit card."
        primaryLabel="Try it free →"
        primaryHref="/jobs/new"
      />
      <PublicSiteFooter />
    </div>
  );
}
